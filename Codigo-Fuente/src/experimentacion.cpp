#include "experimentacion.h"
#include "tree.h"
using namespace std;

namespace fs = filesystem;

chrono::microseconds crearArbolDesdeFS(NodoArbol* raiz, const std::string& base) {

}

chrono::microseconds testBusqueda(NodoArbol* raiz, const vector<string>& nombres) {

}

chrono::microseconds testEliminacion(NodoArbol* raiz, const vector<string>& nombres) {

}

chrono::microseconds testInsercion(NodoArbol* raiz, const vector<string>& carpetas, const vector<string>& nombres, const vector<string>& extensiones) {

}
